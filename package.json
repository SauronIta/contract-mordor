{
  "name": "star-atlas-monitor",
  "version": "1.0.0",
  "description": "Monitor ordini Star Atlas con alert Discord",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "postinstall": "npx playwright install-deps && npx playwright install chromium",
    "start": "node server.mjs"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-fetch": "^3.3.1",
    "playwright": "^1.39.0"
  }
}
